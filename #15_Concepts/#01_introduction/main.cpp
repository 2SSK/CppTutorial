#include <bits/stdc++.h>
using namespace std;

// NOTE: Concepts : A mechanism to place constraints on your template type
// parameters

/* NOTE:
 *  Core language concepts
 *  # same_as : specifies that a type is the same as another type
 *  # derived_from : specifies that a type is derived from another type
 *  # convertible_to : specifies that a type is implicitly convertible to
 *                     another type
 * # common_reference_with : specifies that two types share a
 *                           common reference type
 * # common_with : specifies that two types share a common type
 * # integral : specifies that a type is an integral type
 * # signed_integral : specifies that a type is an integral type that is signed
 * # unsigned_integral : specifies that a type is an integral type that is
 *                       unsigned
 * # floating_point : specifies that a type is a floating-point type
 */

int main() {
  cout << endl;

  return 0;
}
